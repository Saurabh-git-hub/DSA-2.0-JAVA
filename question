*Question:*
Create a class called Car that models the following real-world scenario:

The Car class should have three attributes:

make (String) - represents the manufacturer of the car.

model (String) - represents the model of the car.

year (int) - represents the year of manufacture.

The Car class should also have the following methods:

startEngine() - Prints "Engine started" when called.

stopEngine() - Prints "Engine stopped" when called.

displayDetails() - Prints out the car's details in the following format:

Car make: <make>, model: <model>, year: <year>
Then, create a Main class with the main method:

Instantiate three Car objects with different attributes.

Call the startEngine() method on one of the cars.

Call the stopEngine() method on another car.

Call the displayDetails() method on all the cars.